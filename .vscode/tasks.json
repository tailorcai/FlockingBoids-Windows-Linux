{
    "tasks": [
        {
            "type": "shell",
            "label": "Build",
            "command": "make -f Makefile.osx",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": ["$gcc"],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ],
    "version": "2.0.0"
}
